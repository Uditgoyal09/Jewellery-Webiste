{
  "name": "ganesh-jewellers",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run build --prefix frontend && npm run dev --prefix backend",
    "frontend:build": "npm run build --prefix frontend",
    "frontend:watch": "npm run build:watch --prefix frontend",
    "backend:dev": "npm run dev --prefix backend",
    "backend:start": "npm run start --prefix backend"
  }
}
